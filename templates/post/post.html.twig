{% extends('base.html.twig') %}

{% block body %}
    <h3>{{ post.title }}</h3>
    <p>{{ post.body }}</p>
    <span class="badge bg-warning rounded-pill">Polubienia: {{ post.likes.count }}</span>
{% endblock %}